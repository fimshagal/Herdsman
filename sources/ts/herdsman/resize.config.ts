import { Vector2 } from "../vector.2";
import { IResizeConfig } from "./herdsman.interfaces";

export const resizeConfig: IResizeConfig = {
    collectedAnimalsCounter: new Vector2(335, -350),
    background: new Vector2(-512, -512),
};